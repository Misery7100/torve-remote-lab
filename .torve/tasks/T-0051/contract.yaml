schema_version: 1
id: T-0051
role: implement
intent: >-
  Add a luhn_valid(digits) function to src/lab/__init__.py returning True when the digit string passes the Luhn checksum, False otherwise, ValueError on non-digit input, with unittest coverage under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
