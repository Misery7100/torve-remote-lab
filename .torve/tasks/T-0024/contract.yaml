schema_version: 1
id: T-0024
role: implement
intent: >-
  Add a char_count(text) function to src/lab/__init__.py returning the
  number of non-whitespace characters, with unittest coverage under
  tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
