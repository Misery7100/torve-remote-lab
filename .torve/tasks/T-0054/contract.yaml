schema_version: 1
id: T-0054
role: implement
intent: >-
  Add an rle_decode(text) function to src/lab/__init__.py inverting rle_encode, with unittest coverage under tests/ including a round-trip property over several strings. Existing functions stay untouched.
depends_on: [T-0053]
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
