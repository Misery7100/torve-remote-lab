schema_version: 1
id: T-0007
role: implement
intent: >-
  Add a shout(text) function to src/lab/ returning the text upper-cased
  with a trailing exclamation mark, with unittest coverage under tests/.
  The existing greet() and count_words() stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
