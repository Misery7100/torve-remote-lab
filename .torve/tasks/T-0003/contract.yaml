schema_version: 1
id: T-0003
role: implement
intent: >-
  The battery drives a container: acceptance starts a real container over
  the host daemon from inside the gates sandbox — the A-24 exit criterion
  taken literally.
scope:
  allow: []
  deny: []
acceptance:
  - docker run --rm alpine:3.20 echo driven-from-the-gates-sandbox
decisions: []
tier: executor
