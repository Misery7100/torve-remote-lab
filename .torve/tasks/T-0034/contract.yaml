schema_version: 1
id: T-0034
role: implement
intent: >-
  Add a remove_punctuation(text) function to src/lab/__init__.py
  returning the text with all ASCII punctuation characters removed and
  everything else untouched, with unittest coverage under tests/.
  Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
