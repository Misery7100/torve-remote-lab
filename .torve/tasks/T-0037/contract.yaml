schema_version: 1
id: T-0037
role: implement
intent: >-
  Add an is_isogram(text) function to src/lab/__init__.py returning
  True when no letter repeats case-insensitively, ignoring every
  non-letter character, with unittest coverage under tests/. Existing
  functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
