schema_version: 1
id: T-0045
role: implement
intent: >-
  Add a median(numbers) function in a new module src/lab/stats.py returning the statistical median of a non-empty list, raising ValueError on an empty list, with unittest coverage under tests/. Existing modules stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
