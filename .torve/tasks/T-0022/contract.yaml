schema_version: 1
id: T-0022
role: implement
intent: >-
  Add a word_lengths(text) function to src/lab/__init__.py returning the
  list of lengths of whitespace-separated words, with unittest coverage
  under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
