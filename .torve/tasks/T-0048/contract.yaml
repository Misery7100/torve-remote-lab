schema_version: 1
id: T-0048
role: implement
intent: >-
  Add a hamming_distance(a, b) function to src/lab/__init__.py returning the number of differing positions between two equal-length strings, raising ValueError on unequal lengths, with unittest coverage under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
