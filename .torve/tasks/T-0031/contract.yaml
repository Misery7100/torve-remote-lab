schema_version: 1
id: T-0031
role: implement
intent: >-
  Add an is_pangram(text) function to src/lab/__init__.py returning True
  when the text uses every letter of the English alphabet at least once,
  case-insensitively, with unittest coverage under tests/. Existing
  functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
