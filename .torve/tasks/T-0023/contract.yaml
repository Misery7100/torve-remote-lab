schema_version: 1
id: T-0023
role: implement
intent: >-
  Add a longest_word(text) function to src/lab/__init__.py returning the
  longest whitespace-separated word (first on ties, empty string for
  empty input), with unittest coverage under tests/. Existing functions
  stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
