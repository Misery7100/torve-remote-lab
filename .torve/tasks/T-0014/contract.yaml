# Re-mint of T-0011, abandoned after its rebase conflicted with T-0010's
# landing (D-6.10: re-queue or abandon; the branch could not force-push).
schema_version: 1
id: T-0014
role: implement
intent: >-
  Add an is_palindrome(text) function to src/lab/ that ignores case and
  spaces, with unittest coverage under tests/. Existing functions stay
  untouched.
depends_on: [T-0010]
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
