schema_version: 1
id: T-0018
role: implement
intent: >-
  Add a titlecase(text) function in a new module src/lab/textutil.py
  returning the text with each word capitalized, with unittest coverage
  in a new tests/test_textutil.py. Existing files stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
