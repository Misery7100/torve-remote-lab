schema_version: 1
id: T-0050
role: implement
intent: >-
  Add a from_roman(text) function to src/lab/__init__.py converting a Roman numeral string back to an integer, with unittest coverage under tests/ including a round-trip against roman_numeral. Existing functions stay untouched.
depends_on: [T-0049]
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
