schema_version: 1
id: T-0038
role: implement
intent: >-
  Add a running_total(numbers) function to src/lab/__init__.py returning
  the list of cumulative sums, an empty list for empty input, with
  unittest coverage under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
