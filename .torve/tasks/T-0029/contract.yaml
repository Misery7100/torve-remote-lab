schema_version: 1
id: T-0029
role: implement
intent: >-
  Add an initials(text) function to src/lab/__init__.py returning the
  first letter of each whitespace-separated word upper-cased and joined,
  with unittest coverage under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
