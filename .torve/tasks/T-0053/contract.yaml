schema_version: 1
id: T-0053
role: implement
intent: >-
  Add an rle_encode(text) function to src/lab/__init__.py run-length encoding a string as character-count pairs like a3b1, empty string encoding to an empty string, with unittest coverage under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
