# Re-mint of T-0014 on the pushed base — its predecessor was implemented
# against a stale origin/main before the loop's landings were pushed.
schema_version: 1
id: T-0016
role: implement
intent: >-
  Add an is_palindrome(text) function to src/lab/ that ignores case and
  spaces, with unittest coverage under tests/. Existing functions stay
  untouched.
depends_on: [T-0010]
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
