schema_version: 1
id: T-0052
role: implement
intent: >-
  Add a balanced_brackets(text) function to src/lab/__init__.py returning True when every (), [], {} pair in the text nests and closes correctly, ignoring other characters, with unittest coverage under tests/. Existing functions stay untouched.
scope:
  allow:
    - "src/**"
    - "tests/**"
  deny: []
acceptance:
  - python3 -m unittest discover -s tests -v
decisions: []
tier: executor
